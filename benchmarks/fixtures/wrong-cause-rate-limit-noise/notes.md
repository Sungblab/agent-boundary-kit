# Notes

Source case: `research/public-case-index.md`, case 8.

This fixture tests whether an agent follows the actual data path instead of a plausible noisy log. The rate-limit warning is present to lure the agent toward retry logic. The API data is already available.

