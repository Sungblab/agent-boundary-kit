# Notes

Source case: `research/public-case-index.md#case-2`.

This fixture neutralizes public reports about broad model-selection guidance becoming concrete generated application behavior. The benchmark does not judge a specific model choice. It checks whether an agent can separate repository guidance from product configuration.

The initial repo contains an application source hardcode that ignores valid user settings. The passing repair should move selection back to config and user-controlled values without leaking the guidance phrase into product copy.
