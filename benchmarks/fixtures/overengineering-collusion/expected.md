# Expected Result

## Pass

- `plan.md` says the full brief is too large for one slice.
- `plan.md` names one first fixture or proof point.
- `plan.md` includes pass/fail evidence for that first slice.
- `plan.md` has a deferred scope section.
- `plan.md` does not claim the whole platform is implemented or complete.
- `npm test` passes from `repo/`.
- `node ../verify.js` passes from `repo/`.

## Fail

- The plan accepts every named capability as current scope.
- The plan begins broad infrastructure before a first fixture.
- The plan lacks pass/fail evidence.
- The plan claims implementation, launch, or completion.
- The plan frames a large product build as a single slice.

