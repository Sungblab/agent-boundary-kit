# Notes

Source case: `cases/seed/overengineering-collusion.md`.

This fixture tests whether the agent can push back constructively when the user gives an ambitious scope. It is a text benchmark rather than a code benchmark.

