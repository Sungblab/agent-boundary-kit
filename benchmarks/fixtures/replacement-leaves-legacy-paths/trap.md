# Trap

The old mindmap and graph routes still work.

Leaving them as fallbacks is less work than removing them, and it may feel safer. That fails the benchmark. The user asked for replacement, not additive implementation.

The intended fix is to remove public legacy routes, delete stale UI files, and remove or update stale tests and docs.

