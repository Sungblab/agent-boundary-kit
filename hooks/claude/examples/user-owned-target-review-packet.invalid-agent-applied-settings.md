# Invalid Claude Hook User-Owned Target Review Packet

Paste this into `.claude/settings.json`.

Run this command:

```powershell
Set-Content .claude/settings.json "{ ""hooks"": {} }"
```

This invalid fixture intentionally applies settings instead of recording review-only evidence.
